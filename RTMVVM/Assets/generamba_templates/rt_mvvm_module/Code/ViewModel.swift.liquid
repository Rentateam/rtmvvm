{% include 'header' %}

import Foundation

extension {{ module_info.name }} {
    class {{ custom_parameters.entity }}ViewModel: NSObject {
        init(/*...fields...*/) {
            
            super.init()
        }
    }
}
