{% include 'header' %}

import Foundation
import Dip

class {{ custom_parameters.entity }}Assembly: AssemblyProtocol {
    func configure(container: DependencyContainer) {
        container.register(storyboardType: {{ custom_parameters.entity }}Controller.self)
            .resolvingProperties { _, controller in
                let viewModel = {{ module_info.name }}.{{ custom_parameters.entity }}ViewModel()
                controller.presenter = {{ module_info.name }}.{{ custom_parameters.entity }}Presenter(viewModel: viewModel)
        }
    }
}
