{% include 'header' %}

import Foundation

struct {{ module_info.name }} {}

extension {{ module_info.name }} {
    class {{ custom_parameters.entity }}Presenter {
        //Change to var or many view models if needed
        let viewModel: {{ module_info.name }}.{{ custom_parameters.entity }}ViewModel
        
        init(viewModel: {{ module_info.name }}.{{ custom_parameters.entity }}ViewModel) {
            self.viewModel = viewModel
        }
    }
}
